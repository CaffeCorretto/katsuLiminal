let skinName = 'katsuLiminal'
let exportName = '~ katsuLiminal ~'

let sourceFolder = 'skinFiles/'
let exportFolder = 'dist'


// Pls Don't touch me :(
let outputSource = exportFolder + '/' + exportName + '.osk'

module.exports = {
  skinName,
  exportName,
  sourceFolder,
  exportFolder,
  outputSource
}